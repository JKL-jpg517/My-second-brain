$b$ can be split into two perpendicular parts: $p$ in the column space and $e$, which is perpendicular to the column space
	so $Ax=p+e$ $\implies$ $(||Ax-b||)^{2}=(||Ax-p||)^{2}+(||e||)^{2}$
	the closest: $||Ax-p||=0$
	$\implies$ the colsest solution: $\hat{x}=(A^{T}A)^{-1}A^{T}b$