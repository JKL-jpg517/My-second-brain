just  a strong executable program; convinent text editing! (same as `cd` `ls`  `python.exe`) $\implies$ command is vim + argument!
help siplify programming in terminal！

e.g `vim+文件路径`：open a file in vim editor
`vim`: go directly into the 主页面
vim program can also install extentions!